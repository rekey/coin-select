{
  "url": "https://bapi.beasi.top/webhook",
  "comment": "服务器 webhook 端点。留空 = 未启用；token 配置在 GitHub repository secret WEBHOOK_TOKEN (Settings -> Secrets and variables -> Actions)，认证 header 为 X-Webhook-Token。"
}
